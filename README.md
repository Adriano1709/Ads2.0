# Ads2.0
function setup() {
  createCanvas(400, 400);
}function setup() {
  createCanvas(800, 800);
  background("white")
}

function draw() {
  stroke("blue");
  fill("red");
  
  
  if (mouseIsPressed) {
    rect(mouseX, mouseY, 20, 35);
  }
}r
